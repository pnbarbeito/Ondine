<?php
/**
 * IDE-only stub: declare global Env for static analyzers (Intelephense)
 *
 * This file is intentionally wrapped in `if (false)` so it is parsed by
 * language servers but never executed at runtime. Runtime compatibility is
 * provided by `src/compat.php` (loaded via composer `autoload.files`).
 */
if (false) {
    /**
     * @noinspection PhpUnused
     */
    class Env extends \Ondine\Env {}
}
